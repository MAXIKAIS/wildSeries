{{ form_start(form) }}
<div class="container">
    {{ form_widget(form) }}
    <button class="btn bg-dark text-mainColorText">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
